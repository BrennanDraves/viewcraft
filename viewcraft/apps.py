"""Config for the viewcraft app."""

from django.apps import AppConfig


class ViewcraftConfig(AppConfig):
    """Config for the viewcraft app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'viewcraft'
